# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
warnings_are_errors: false
cache: packages

r_github_packages: FlorianSchwendinger/pdfminer

branches:
  only:
    - "master"

r:
  - oldrel
  - release
  - devel

os:
  - linux
  - osx 
